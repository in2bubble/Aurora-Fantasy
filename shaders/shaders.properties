# Aurora Fantasy 5.3

# ═══════════════════════════════════════════
#  Profiles: Medium / High / Extreme
# ═══════════════════════════════════════════

# ── EXTREME ── No limits. Everything maxed. Triple-A demanding.
profile.EXTREME = \
  SHADOW_CASTING SHADOW_QTY_SLIDER=6 SHADOW_DISTANCE_SLIDER=8 SHADOW_TYPE=1 SHADOW_BLUR=5.0 \
  SHADOW_ENTITIES COLORED_SHADOW \
  REFLECTION_SLIDER=2 SSR_MAX_STEPS=64 SSR_STEP_SIZE=0.4 SSR_BINARY_STEPS=10 SSR_STRENGTH=10 \
  AO=1 AOSTEPS=10.0 AO_STRENGTH=2.0 VANILLA_AO=1 \
  AA_TYPE=3 FXAA REFRACTION=1 WAVING=1 \
  V_CLOUDS=2 CIRRUS CLOUD_STEPS_AVG=10 CIRRUS_STEPS_AVG=5 CLOUD_REFLECTION END_CLOUDS CLOUD_DENSITY=1.0 \
  BLOOM BLOOM_SAMPLES=8.0 BLOOM_STRENGTH=1.5 \
  VOL_LIGHT=2 GODRAY_STEPS=7 \
  MATERIAL_GLOSS EMMISIVE_ORE EMMISIVE_MATERIAL DYN_HAND_LIGHT \
  RAIN_PUDDLES RAIN_RIPPLES PUDDLE_COVERAGE=1.0 PUDDLE_REFLECTIVITY=1.0 WET_SURFACE_DARKEN=0.5 \
  WATER_FOAM FOAM_WIDTH=3.0 FOAM_BRIGHTNESS=1.5 \
  BIOME_SKY BIOME_FOG SANDSTORM STAR_SLIDER=2 SUN_REFLECTION=2 END_STARS \
  STARS_BRIGHTNESS=1.0 STARS_COVERAGE=1.0 \
  MOTION_BLUR MOTION_BLUR_SAMPLES=8.0 MOTION_BLUR_STRENGTH=1.0 \
  DOF_STRENGTH=15 POM POM_DEPTH=0.30 \
  FOG_ACTIVE FOG_ADJUST=1.0 \
  NIGHT_BRIGHT=0.50 AVOID_DARK_LEVEL=2.0 OMNI_MUL=0.15 \
  WIND_FORCE=2.00 SHARP_FORCE=1.2

# ── HIGH ── Premium quality. Demanding but not extreme.
profile.HIGH = \
  SHADOW_CASTING SHADOW_QTY_SLIDER=5 SHADOW_DISTANCE_SLIDER=6 SHADOW_TYPE=1 SHADOW_BLUR=5.0 \
  SHADOW_ENTITIES COLORED_SHADOW \
  REFLECTION_SLIDER=2 SSR_MAX_STEPS=32 SSR_STEP_SIZE=0.8 SSR_BINARY_STEPS=8 SSR_STRENGTH=10 \
  AO=1 AOSTEPS=5.0 AO_STRENGTH=1.3 VANILLA_AO=1 \
  AA_TYPE=3 !FXAA REFRACTION=1 WAVING=1 \
  V_CLOUDS=2 CIRRUS CLOUD_STEPS_AVG=5 CIRRUS_STEPS_AVG=5 CLOUD_REFLECTION END_CLOUDS CLOUD_DENSITY=1.0 \
  BLOOM BLOOM_SAMPLES=5.0 BLOOM_STRENGTH=1.2 \
  VOL_LIGHT=2 GODRAY_STEPS=5 \
  MATERIAL_GLOSS EMMISIVE_ORE EMMISIVE_MATERIAL DYN_HAND_LIGHT \
  RAIN_PUDDLES RAIN_RIPPLES PUDDLE_COVERAGE=0.8 PUDDLE_REFLECTIVITY=0.8 WET_SURFACE_DARKEN=0.4 \
  WATER_FOAM FOAM_WIDTH=2.0 FOAM_BRIGHTNESS=1.0 \
  BIOME_SKY BIOME_FOG SANDSTORM STAR_SLIDER=2 SUN_REFLECTION=2 END_STARS \
  STARS_BRIGHTNESS=1.4 STARS_COVERAGE=1.2 \
  !MOTION_BLUR MOTION_BLUR_SAMPLES=5.0 MOTION_BLUR_STRENGTH=0.75 \
  DOF_STRENGTH=0 !POM POM_DEPTH=0.15 \
  FOG_ACTIVE FOG_ADJUST=2.0 \
  NIGHT_BRIGHT=0.60 AVOID_DARK_LEVEL=4.0 OMNI_MUL=0.3 \
  WIND_FORCE=1.25 SHARP_FORCE=0.95

# ── MEDIUM ── Balanced and friendly. Beautiful without stress.
profile.MEDIUM = \
  SHADOW_CASTING SHADOW_QTY_SLIDER=3 SHADOW_DISTANCE_SLIDER=4 SHADOW_TYPE=1 SHADOW_BLUR=3.0 \
  SHADOW_ENTITIES !COLORED_SHADOW \
  REFLECTION_SLIDER=1 SSR_MAX_STEPS=8 SSR_STEP_SIZE=1.6 SSR_BINARY_STEPS=4 SSR_STRENGTH=6 \
  AO=0 AOSTEPS=2.0 AO_STRENGTH=1.0 VANILLA_AO=1 \
  AA_TYPE=3 !FXAA REFRACTION=1 WAVING=1 \
  V_CLOUDS=1 CIRRUS CLOUD_STEPS_AVG=3 CIRRUS_STEPS_AVG=3 !CLOUD_REFLECTION END_CLOUDS CLOUD_DENSITY=1.0 \
  BLOOM BLOOM_SAMPLES=2.0 BLOOM_STRENGTH=0.9 \
  VOL_LIGHT=1 GODRAY_STEPS=3 \
  MATERIAL_GLOSS EMMISIVE_ORE EMMISIVE_MATERIAL DYN_HAND_LIGHT \
  RAIN_PUDDLES !RAIN_RIPPLES PUDDLE_COVERAGE=0.4 PUDDLE_REFLECTIVITY=0.5 WET_SURFACE_DARKEN=0.2 \
  WATER_FOAM FOAM_WIDTH=1.0 FOAM_BRIGHTNESS=0.6 \
  BIOME_SKY BIOME_FOG !SANDSTORM STAR_SLIDER=1 SUN_REFLECTION=1 !END_STARS \
  STARS_BRIGHTNESS=0.8 STARS_COVERAGE=0.7 \
  !MOTION_BLUR MOTION_BLUR_SAMPLES=3.0 MOTION_BLUR_STRENGTH=0.5 \
  DOF_STRENGTH=0 !POM POM_DEPTH=0.10 \
  FOG_ACTIVE FOG_ADJUST=4.0 \
  NIGHT_BRIGHT=0.55 AVOID_DARK_LEVEL=6.0 OMNI_MUL=0.45 \
  WIND_FORCE=0.75 SHARP_FORCE=0.80

# ═══════════════════════════════════════════
#  Settings Menu
# ═══════════════════════════════════════════

screen.columns = 1
screen = \
    <profile> \
    <empty> \
    DISTANT_RENDER_MOD \
    <empty> \
    [SHADOWS_SCREEN] \
    [ATMOSPHERE_SCREEN] \
    [WATER_SCREEN] \
    [EFFECTS_SCREEN] \
    [CAMERA_SCREEN] \
    [CUSTOM_COLORS_SCREEN]

# ── Shadows & Lighting ────────────────────
screen.SHADOWS_SCREEN.columns = 2
screen.SHADOWS_SCREEN = \
    SHADOW_CASTING SHADOW_ENTITIES \
    SHADOW_QTY_SLIDER SHADOW_DISTANCE_SLIDER \
    SHADOW_TYPE SHADOW_BLUR \
    COLORED_SHADOW sunPathRotation \
    <empty> <empty> \
    VOL_LIGHT GODRAY_STEPS \
    <empty> <empty> \
    AO VANILLA_AO \
    AO_STRENGTH OMNI_MUL \
    <empty> <empty> \
    BLOCKLIGHT_TEMP DYN_HAND_LIGHT \
    NIGHT_BRIGHT AVOID_DARK_LEVEL

# ── Sky & Atmosphere ──────────────────────
screen.ATMOSPHERE_SCREEN.columns = 2
screen.ATMOSPHERE_SCREEN = \
    COLOR_SCHEME <empty> \
    <empty> <empty> \
    V_CLOUDS USE_CLOUD_VOL_STYLE \
    CLOUD_DENSITY CLOUD_STEPS_AVG \
    CIRRUS CIRRUS_STEPS_AVG \
    CLOUD_REFLECTION END_CLOUDS \
    <empty> <empty> \
    BIOME_SKY BIOME_FOG \
    FOG_ACTIVE FOG_ADJUST \
    SANDSTORM <empty> \
    <empty> <empty> \
    STAR_SLIDER STARS_BRIGHTNESS \
    STARS_COVERAGE END_STARS

# ── Water & Weather ───────────────────────
screen.WATER_SCREEN.columns = 2
screen.WATER_SCREEN = \
    REFLECTION_SLIDER SUN_REFLECTION \
    SSR_MAX_STEPS SSR_STRENGTH \
    REFRACTION <empty> \
    <empty> <empty> \
    USE_WATER_TEXTURE WATER_ABSORPTION \
    WATER_FOG USE_WATER_TURBULENCE \
    WATER_FOAM FOAM_WIDTH \
    <empty> <empty> \
    RAIN_PUDDLES RAIN_RIPPLES \
    PUDDLE_COVERAGE PUDDLE_REFLECTIVITY \
    WET_SURFACE_DARKEN WEATHER_OPACITY

# ── Effects ───────────────────────────────
screen.EFFECTS_SCREEN.columns = 2
screen.EFFECTS_SCREEN = \
    BLOOM BLOOM_STRENGTH \
    MOTION_BLUR MOTION_BLUR_STRENGTH \
    DOF_STRENGTH <empty> \
    POM_DEPTH <empty> \
    MATERIAL_GLOSS WAVING \
    EMMISIVE_ORE EMMISIVE_MATERIAL \
    WIND_FORCE <empty> \
    AA_TYPE FXAA

# ── Camera & Colors ───────────────────────
screen.CAMERA_SCREEN.columns = 2
screen.CAMERA_SCREEN = \
    STYLE TONEMAPPING \
    SATURATION CONTRAST \
    BRIGHTNESS GAMMA \
    VIBRANCE EXPOSURE \
    SHARP_FORCE <empty>

# ── Custom Colors ─────────────────────────
screen.CUSTOM_COLORS_SCREEN.columns = 2
screen.CUSTOM_COLORS_SCREEN = \
    [CUSTOM_COLORS_DAY] [CUSTOM_COLORS_SUNSET] \
    [CUSTOM_COLORS_NIGHT] [CUSTOM_COLORS_WATER] \
    NIGHT_BRIGHT_RANGE <empty> \
    RED GREEN \
    BLUE <empty>

screen.CUSTOM_COLORS_DAY.columns = 2
screen.CUSTOM_COLORS_DAY = \
    LIGHT_DAY_COLOR_R LIGHT_DAY_COLOR_G \
    LIGHT_DAY_COLOR_B <empty> \
    ZENITH_DAY_COLOR_R ZENITH_DAY_COLOR_G \
    ZENITH_DAY_COLOR_B <empty> \
    HORIZON_DAY_COLOR_R HORIZON_DAY_COLOR_G \
    HORIZON_DAY_COLOR_B <empty>

screen.CUSTOM_COLORS_SUNSET.columns = 2
screen.CUSTOM_COLORS_SUNSET = \
    LIGHT_SUNSET_COLOR_R LIGHT_SUNSET_COLOR_G \
    LIGHT_SUNSET_COLOR_B <empty> \
    ZENITH_SUNSET_COLOR_R ZENITH_SUNSET_COLOR_G \
    ZENITH_SUNSET_COLOR_B <empty> \
    HORIZON_SUNSET_COLOR_R HORIZON_SUNSET_COLOR_G \
    HORIZON_SUNSET_COLOR_B <empty>

screen.CUSTOM_COLORS_NIGHT.columns = 2
screen.CUSTOM_COLORS_NIGHT = \
    LIGHT_NIGHT_COLOR_R LIGHT_NIGHT_COLOR_G \
    LIGHT_NIGHT_COLOR_B <empty> \
    ZENITH_NIGHT_COLOR_R ZENITH_NIGHT_COLOR_G \
    ZENITH_NIGHT_COLOR_B <empty> \
    HORIZON_NIGHT_COLOR_R HORIZON_NIGHT_COLOR_G \
    HORIZON_NIGHT_COLOR_B <empty>

screen.CUSTOM_COLORS_WATER.columns = 2
screen.CUSTOM_COLORS_WATER = \
    WATER_COLOR_R WATER_COLOR_G \
    WATER_COLOR_B <empty>

# Default forced options
vignette = false
separateAo = false
oldLighting = false
dynamicHandLight = true
oldHandLight = false
underwaterOverlay = false
sun = true
moon = true
frustum.culling = true
shadow.culling = true
particles.before.deferred = true
sky=true

#ifdef SHADOW_CASTING
    shadowPlayer = true
#endif

#ifdef SHADOW_ENTITIES
    shadowEntities = true
#else
    shadowEntities = false
#endif

#if V_CLOUDS > 0
    clouds = off
#elif V_CLOUDS == 0
    clouds = fancy
#endif

program.world0/composite1.enabled=BLOOM || DOF
program.world-1/composite1.enabled=BLOOM || DOF
program.world1/composite1.enabled=BLOOM || DOF

program.world0/composite3.enabled=false
program.world-1/composite3.enabled=false
program.world1/composite3.enabled=false

texture.gbuffers.noisetex = textures/water_256_RG_8bit.png

#if USE_CLOUD_VOL_STYLE == 0
    texture.gbuffers.gaux2 = textures/clouds_natural_512_R_8bit.png
    texture.deferred.gaux2 = textures/clouds_natural_512_R_8bit.png
#elif USE_CLOUD_VOL_STYLE == 1
    texture.gbuffers.gaux2 = textures/clouds_blocky_512_R_8bit.png
    texture.deferred.gaux2 = textures/clouds_blocky_512_R_8bit.png
#elif USE_CLOUD_VOL_STYLE == -1
    #if STYLE == 1
        texture.gbuffers.gaux2 = textures/clouds_natural_512_R_8bit.png
        texture.deferred.gaux2 = textures/clouds_natural_512_R_8bit.png
    #else
        texture.gbuffers.gaux2 = textures/clouds_blocky_512_R_8bit.png
        texture.deferred.gaux2 = textures/clouds_blocky_512_R_8bit.png
    #endif
#endif

program.shadow.enabled=SHADOW_CASTING
program.world0/shadow.enabled=SHADOW_CASTING
program.world1/shadow.enabled=SHADOW_CASTING
program.world-1/shadow.enabled=false

# Custom uniforms -----

# Many values are precalculated to avoid their later or repetitive calculation.

# Pixel size
uniform.float.pixel_size_x = (1.0 / viewWidth)
uniform.float.pixel_size_y = (1.0 / viewHeight)
uniform.float.inv_aspect_ratio = (1.0 / aspectRatio)
uniform.vec2.resolution = vec2(viewWidth, viewHeight)


# Time constants
variable.float.hour_world = worldTime * 0.001
uniform.float.day_moment = hour_world * 0.04166666666666667

variable.float.continuousWorldDay = fmod(worldDay, 50)
uniform.float.TotalWorldTime = worldTime + (continuousWorldDay * 24000.0)

# Day color mixer (for shadows) - This formula allows graphing the function that is calculated later.
# It allows setting values depending on the time of day. Particularly interesting are the transitions
# from day to night and vice versa, which are often used to indicate the intensity of the shadow
# (when it is produced by the moon instead of the sun), and other similar things.
# f(x) = min(−((x−.25)^2)∙20 + 1.25, 1)
# g(x) = min(−((x−.75)^2)∙50 + 3.125, 1)

variable.float.moment_aux = day_moment - 0.25
variable.float.moment_aux_2 = moment_aux * moment_aux
uniform.float.day_mixer = clamp(-moment_aux_2 * 20.0 + 1.25, 0.0, 1.0)

variable.float.moment_aux_3 = day_moment - 0.75
variable.float.moment_aux_4 = moment_aux_3 * moment_aux_3
uniform.float.night_mixer = clamp(-moment_aux_4 * 50.0 + 3.125, 0.0, 1.0)

# Day color mixer (for volumetric light) - "Similar to the previous case, but applied to values
# dependent on the time of day for volumetric lighting.

variable.float.moment_aux_5 = (day_moment * 4.0) - 1.0
variable.float.moment_aux_6 = moment_aux_5 * moment_aux_5 * moment_aux_5 * moment_aux_5
variable.float.day_vol_mixer = clamp(((-moment_aux_6 + 1.0) * 7.0) + 1.0, 1.0, 8.0)

variable.float.moment_aux_7 = (day_moment * 4.0) - 3.0
variable.float.moment_aux_8 = moment_aux_7 * moment_aux_7 * moment_aux_7 * moment_aux_7
variable.float.night_vol_mixer = clamp(((-moment_aux_8 + 1.0) * 7.0) + 1.0, 1.0, 8.0)

uniform.float.vol_mixer = max(day_vol_mixer, night_vol_mixer)

# Light mix - Similar to the previous cases, a value that depends on the time of day
# to adjust various different parameters.
variable.float.light_mix_a = if((worldTime >= 0 && worldTime < 12485) || worldTime >= 23515, 1.0, 0.0)
variable.float.light_mix_b = if(worldTime >= 12485 && worldTime < 13085, 1.0 - ((worldTime - 12485) * 0.0016666666666666668), 0.0)
variable.float.light_mix_c = 0.0
variable.float.light_mix_d = if(worldTime >= 22915 && worldTime < 23515, (worldTime - 22915) * 0.0016666666666666668, 0.0)
variable.float.light_mix_e = max(light_mix_a, light_mix_b)
variable.float.light_mix_f = max(light_mix_c, light_mix_d)
uniform.float.light_mix = max(light_mix_e, light_mix_f)

# Frame modulus
uniform.int.frame_mod = fmod(frameCounter, 8)

# TAA OFFSET - The offset for TAA is precalculated here.
# It is based on a pentagon (repeated twice as it is a multiple of the 10-frame module.)
# The traversal goes from one vertex of the pentagon to another at the opposite end,
# so that the traversal between different points is maximized and the images vary
# as much as possible from one to the next. It helps to make the flickering of the image
# so fast that it is "absorbed" and better masked by the TAA history.

#if AA_TYPE > 0
    # Pentagon
    uniform.vec2.taa_offset = vec2((if( \
    frame_mod == 0,  0.5, \
    frame_mod == 1, -0.5, \
    frame_mod == 2, -0.5, \
    frame_mod == 3,  0.5, \
    frame_mod == 4,  0.5, \
    frame_mod == 5, -0.5, \
    frame_mod == 6, -0.5, \
    frame_mod == 7,  0.5, \
    0.0)) * pixel_size_x, \
    (if( \
    frame_mod == 0,  0.5, \
    frame_mod == 1, -0.5, \
    frame_mod == 2,  0.5, \
    frame_mod == 3, -0.5, \
    frame_mod == 4,  0.5, \
    frame_mod == 5, -0.5, \
    frame_mod == 6,  0.5, \
    frame_mod == 7, -0.5, \
    0.0)) * pixel_size_y)

    # Dither shift - When Temporal sampling is active, dithering patterns change over time.
    # This only works in recent versions of Minecraft/Optifine/Iris.
    # For older versions, a mechanism that depends on the
    # uniform "frame_mod" is used to calculate the increment.
    uniform.float.dither_shift = if( \
    frame_mod == 0, 0.625, \
    frame_mod == 1, 0.75, \
    frame_mod == 2, 0.375, \
    frame_mod == 3, 0.125, \
    frame_mod == 4, 0.5, \
    frame_mod == 5, 0.25, \
    frame_mod == 6, 0.875, \
    frame_mod == 7, 0.0, \
    0.0)
#endif

# FOV - A precalculated value related to FOV
uniform.float.fov_y_inv = 1.0 / atan(1.0 / gbufferProjection.1.1) * 0.5

# float horizontalFOV = atan(1.0 / gbufferProjection[0].x);
# float verticalFOV = atan(1.0 / gbufferProjection[1].y); <-- fov_y_inv

#ifdef DISTANT_HORIZONS
dhShadow.enabled = false
blend.dh_water=SRC_ALPHA ONE_MINUS_SRC_ALPHA ONE ONE_MINUS_SRC_ALPHA
#endif

uniform.float.swamp= smooth(if(biome_category == CAT_SWAMP, 1, 0), 60, 60)

uniform.float.arid=smooth(if(biome_category == CAT_DESERT || biome_category == CAT_MESA, 1, 0), 60, 60)
uniform.float.jungle=smooth(if(biome_category == CAT_JUNGLE, 1, 0), 60, 60)

uniform.float.pale_garden=smooth(if(in(biome, BIOME_PALE_GARDEN), 1, 0), 60, 60)

uniform.float.snow=smooth(if(biome_category == CAT_ICY, 1, 0.0), 60, 60)
uniform.float.hill=smooth(if(biome_category == CAT_EXTREME_HILLS, 1, 0.0), 60, 60)
uniform.float.taiga=smooth(if(biome_category == CAT_TAIGA, 1, 0), 60, 60)
